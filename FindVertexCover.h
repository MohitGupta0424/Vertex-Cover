#include <iostream>
#include <vector>

using namespace std;

class FindVertexCover{
    public:
        void minVertexCover(int,int,vector<string>);
        int combination(int);
        vector<int> negCombination(int,vector<int>,int,int);
};